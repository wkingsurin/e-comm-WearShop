model Variant {
  id String @id @default(cuid())

  productId String

  sku String @unique

  price    Int
  oldPrice Int?

  stock Int

  color String
  size  String

  product Product @relation(fields: [productId], references: [id])

  images Image[]
}
